# Overlaps

An overlap declares compatibility between two contexts using eight fixed check
types documented in the [compatibility reference](../reference/compatibility-checks.md).
Operands are confined project files and optional JSON Pointers. Checks do not
execute expressions, scripts, templates, or manifest-provided commands.
