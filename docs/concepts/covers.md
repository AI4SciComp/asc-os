# Declared covers

A cover names a target, member contexts, a requirement-to-context matrix, and
required overlaps. `cover check` verifies only that declared structure and its
project boundaries. It does not infer that an undeclared scientific requirement
is complete.

Glue additionally requires ready context status, passing overlaps, valid claim
policy, resolved evidence, and fresh derived state.
