# ADR 0001: project identity

Status: accepted, 2026-08-29.

The canonical repository and Python distribution are `asc-os`, the import is
`asc_os`, and the CLI is `asc-os`. One generic orchestration repository avoids
split identity and conflicting releases. No compatibility alias is shipped.
