# Claude Code guide

`asc-os context build CTX-ID --harness claude` emits a neutral common model,
prompt, and `CLAUDE.fragment.md`. It does not overwrite a root `CLAUDE.md`.
Harness adapters contain the same bounded state and differ only in presentation.
