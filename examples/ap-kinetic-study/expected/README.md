# Golden verification data

`golden-hashes.json` records byte hashes for a Codex `CTX-ORBIT` bundle built
from a clean non-Git copy of this synthetic project. The incompatible overlap
fixture is intentionally outside canonical `research/` state.
