# Research project

This repository's canonical authored research state lives under `research/`.
Generated harness bundles live under `.ai/generated/` and are never the source
of truth. Run `asc-os validate` before treating declared overlaps or claims as
compatible.

